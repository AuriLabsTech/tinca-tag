"""Rendering: Endpoint to deep link and QR (ADR 0001, 0002).

Consumes an :class:`~tinca_tag.endpoint.Endpoint` and produces the
``tinca://connect?...`` URL and the QR (ASCII / PNG / SVG, optionally branded).
Offline by default: no network calls.

Not implemented yet. See milestone M1.
"""
